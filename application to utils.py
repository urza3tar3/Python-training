import utils
from utils import find_max
numbers = [1,9,3,4,5]
print(utils.find_max(numbers))
print(find_max([1,9,3,4,10]))
#purple is the color used to identify the build in functions
print(max(numbers))