def square(number):
    return number * number #rif we dont use return the function by default returns None
#we use the return statement if there's a function and u need to return a value or the result if teh functions does some calculations
result = square(3)
print(result)
